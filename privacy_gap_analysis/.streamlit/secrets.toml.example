# Copy this file to `.streamlit/secrets.toml` for local development, or paste
# these keys into the Streamlit Community Cloud "Secrets" panel for deployment.
#
# NEVER commit the real secrets.toml file — it's listed in .gitignore.

OPENAI_API_KEY = "sk-replace-with-your-real-key"
